const fs=require('fs');const s=fs.readFileSync('src/app/cart/page.js','utf8');const parts=[];let re=/(<\/div>)|(<div\b[^>]*>)/g;let m;while(m=re.exec(s)){const line = s.substr(0,m.index).split(/\r?\n/).length;parts.push({text:m[0],index:m.index, line});}let stack=[];for(let i=0;i<parts.length;i++){const p=parts[i];if(p.text.startsWith('<div')){stack.push(p);console.log('OPEN <div> at line',p.line);}else{console.log('CLOSE </div> at line',p.line);const last=stack.pop();if(!last){console.log('Unmatched closing </div> at index',p.index);break;}}}if(stack.length){console.log('Unclosed <div> occurrences remaining:',stack.length,'first at line:', stack[0].line);console.log('Context:');const lines=s.split(/\r?\n/);const ln=Math.max(1,stack[0].line);for(let i=Math.max(1,ln-6);i<=Math.min(lines.length,ln+6);i++) console.log(i+':',lines[i-1]);}else{console.log('All divs matched');}