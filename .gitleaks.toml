# Gitleaks configuration - whitelist template files with placeholders

[[allowlist]]
  description = ".env.example is a template with placeholder values only"
  paths = [".env.example"]
  regexes = ["DATABASE_URL="]

[[allowlist]]
  description = "Documentation files, not secrets"
  paths = ["SECURITY_ROTATION.md", "DATABASE_MIGRATION.md"]
